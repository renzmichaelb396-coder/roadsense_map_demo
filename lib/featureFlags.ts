// lib/featureFlags.ts

/**
 * Global feature flags for RoadSense PH
 * Locked features must not be toggled casually
 */
export const FEATURES = {
  USE_SUPABASE_HAZARDS: true, // ENABLED post-ship
};
