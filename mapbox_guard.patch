diff --git a/app/dashboard/map/page.inner.tsx b/app/dashboard/map/page.inner.tsx
index 0000000..1111111 100644
--- a/app/dashboard/map/page.inner.tsx
+++ b/app/dashboard/map/page.inner.tsx
@@ -37,9 +37,10 @@ export default function MapPageInner({ hazards }: { hazards: Hazard[] }) {
   useEffect(() => {
     if (!containerRef.current) return;

-    if (!process.env.NEXT_PUBLIC_MAPBOX_TOKEN) {
-      throw new Error("Mapbox token missing");
-    }
+    if (!process.env.NEXT_PUBLIC_MAPBOX_TOKEN) {
+      console.warn("Mapbox token missing — map disabled");
+      return;
+    }

     mapboxgl.accessToken = process.env.NEXT_PUBLIC_MAPBOX_TOKEN;

@@
-  return <div ref={containerRef} style={{ height: "100vh", width: "100%" }} />;
+  if (!process.env.NEXT_PUBLIC_MAPBOX_TOKEN) {
+    return (
+      <div
+        style={{
+          height: "100vh",
+          width: "100%",
+          display: "flex",
+          alignItems: "center",
+          justifyContent: "center",
+          background: "#000",
+          color: "#fff",
+          fontSize: 14,
+          opacity: 0.85,
+          padding: 24,
+          textAlign: "center",
+        }}
+      >
+        Map view is temporarily unavailable.
+        <br />
+        Please contact your system administrator.
+      </div>
+    );
+  }
+
+  return <div ref={containerRef} style={{ height: "100vh", width: "100%" }} />;
 }
