import { View, Text } from "react-native";

export default function MapTab() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Map view is available on mobile only.</Text>
    </View>
  );
}
